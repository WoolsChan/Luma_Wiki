---
import "@/styles/tailwind.css";
import { Image } from "astro:assets";
const { src, alt, tailwind } = Astro.props;
---

<!-- rounded-lg border [border-color:#ff69b4] -->
<Image src={src} alt={alt} class={`justify-self-center ${tailwind}`} />
